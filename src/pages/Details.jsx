

const Details = () => {
    // const { product_title, product_image,price } = data || {};{ data }
    return (
        <div>
            <p>hello</p>
        </div>
    );
};

// export default Details;